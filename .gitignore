# Worktrees
.worktrees/

# MCP config (contains secrets)
.mcp.json

# Claude Code local settings (contains MCP tokens)
.claude/settings.local.json

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env
*.env*
!.env.example

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Node
node_modules/
dist/

#DB
.conversasreais
.rags.claude/

# Logs (keep directory, ignore log files)
logs/*.log

# Redis config com senha (gerado na VPS, nao commitar)
backend/redis.conf

# Override de compose dev-only (publica porta do Redis no host) — nunca em producao
backend/docker-compose.override.yml
docker-compose.override.yml

# Vercel e arquivos de ambiente (nunca commitar)
.vercel
.env*
