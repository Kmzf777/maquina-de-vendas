fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.34.0,<1.0
redis>=5.0.0,<6.0
supabase>=2.28.0,<3.0
httpx>=0.28.0,<1.0
python-dotenv>=1.0.0,<2.0
python-multipart>=0.0.20,<1.0
pydantic-settings>=2.5.0,<3.0
python-dateutil>=2.9.0,<3.0
google-genai>=1.0.0
google-generativeai>=0.8.0  # DEPRECATED — usado só por scripts/rehearsal/gemini_actor.py (harness local)
PyJWT>=2.8.0,<3.0
sentry-sdk[fastapi]>=2.0,<3.0  # fail-open: sem SENTRY_DSN o init é no-op (app/observability.py)
# PDF do orçamento (app/quotes/pdf.py). Wheel puro + pillow, sem biblioteca de
# sistema — não exige nada novo no Dockerfile.
reportlab>=4.2,<5
# Windows nao possui a base IANA de fusos; zoneinfo (stdlib) precisa do pacote tzdata.
tzdata; sys_platform == 'win32'
