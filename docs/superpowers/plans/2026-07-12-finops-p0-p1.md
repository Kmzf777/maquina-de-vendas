# Plano de implementação — FinOps P0+P1

Spec: `docs/superpowers/specs/2026-07-12-finops-p0-p1-design.md` · Branch: `finops/p0-p1-thinking-cache` → push direto p/ `master` (autorizado pelo usuário nesta sessão, após suíte verde).

## Tarefa 1 — base.py: fatiar estático × volátil (P1)
1. Extrair o corpo `<role>…</examples>` do f-string de `build_base_prompt` para constante de módulo `BASE_STATIC` (string plana — trecho verificado sem placeholders e sem `{{`).
2. Mover a lógica volátil (greeting/data, `name_instruction`, `company_line`, `<crm_data>`, `<lead_memory>`) para `build_context_block(lead_name, lead_company, now, lead_context) -> str`, retornando o bloco `<context>…</context>` com o MESMO template atual.
3. Reimplementar `build_base_prompt` = `BASE_STATIC + "\n\n" + build_context_block(...)` (byte-idêntico; compat com todos os chamadores/testes existentes).
4. Atualizar o comentário de design do caching (a ordem completa agora é garantida no orchestrator).

## Tarefa 2 — orchestrator.py: nova ordem do system prompt (P1)
1. Import: `BASE_STATIC`, `build_context_block` (mantém `FINAL_INSTRUCTION`).
2. `build_system_prompt`: `[BASE_STATIC, stage_prompt, catálogo?, context_block, FINAL_INSTRUCTION]` join `"\n\n"`.
3. Comentário: documentar o invariante "nenhum byte volátil antes do fim do catálogo".

## Tarefa 3 — orchestrator.py: política de thinking (P0-A)
1. `_initial_thinking_off`: default do env `"on"` → `"off"` (+ docstring com a inversão e a data).
2. Capturar `initial_thinking_off` antes da 1ª chamada; no retry-on-empty silencioso usar a política oposta: `thinking_off = _thinking_off_for(model) if not initial_thinking_off else False`.
3. Atualizar o comentário do bloco RETRY-ON-EMPTY (o retry agora é o degrau COM thinking quando a inicial rodou sem).

## Tarefa 4 — budget_guard.py: kill-switch armado (P0-B)
1. `daily_cost_limit_usd`: default `"0"` → `"8"` (+ docstrings do módulo/função).

## Tarefa 5 — envs
1. `.env.example`: `LLM_DAILY_COST_LIMIT_USD=8`, `LLM_INITIAL_THINKING=off` + comentários da nova política.
2. `.env.local` (dev, não versionado): mesmos valores.

## Tarefa 6 — rehearsal.yml
1. Cron `0 9 * * *` → `0 9 * * 2,5`; comentário atualizado (custo/run).
2. `gh secret set REHEARSAL_SUPABASE_URL` / `REHEARSAL_SUPABASE_SERVICE_KEY` (valores homolog do `.env.local`). Gemini secrets: pendência do usuário (documentar no relatório final).

## Tarefa 7 — testes
1. Atualizar `test_llm_cost_guards_2026_07_07.py`: default thinking = off; default budget = 8 (e `0` explícito desarma).
2. Atualizar `test_gemini_thinking_off_post_tool.py`: baseline nova (1ª chamada thinking_off=True) + variante `LLM_INITIAL_THINKING=on` cobrindo o rollback.
3. Criar `test_finops_p0_p1_2026_07_12.py` (ordem/prefixo do system prompt + fallback condicional do retry + budget default).

## Tarefa 8 — validação e deploy
1. `python -m pytest` (backend, suíte completa) — verde obrigatório.
2. `git pull origin master` → resolver conflitos se houver → `git push origin finops/p0-p1-thinking-cache:master` (dispara deploy).
