# Plano — FinOps P2+P3+Higiene

Spec: `docs/superpowers/specs/2026-07-12-finops-p2-p3.md` · Branch: `finops/p2-p3-secagem` → master (autorizado após suíte verde).

## Trilha B — Engenharia & Histórico (executor: sessão principal)
1. **SUMMARY_MODEL**: `config.py` (+`.env.example`), `tools.py` (caller do handoff usa `settings.summary_model` em vez de `DEFAULT_MODEL`). Teste.
2. **Histórico**: `service.get_history` ganha `roles: tuple|None` (filtro na query) e `char_budget: int|None` (descarta as linhas mais antigas acima do teto, aplicado após ordenação). `orchestrator.run_agent` passa `roles=("user","assistant")` e `char_budget=settings.history_char_budget` (env `HISTORY_CHAR_BUDGET`, default 36000, 0 desliga). Testes: filtro, cap, contrato antigo intacto.
3. **Dedup de tool-call por turno** (orchestrator, loop ReAct + Change B): cache `{(name, json_args): result}`; repetida → devolve cacheado sem executar (log `[TOOL DEDUP]`); iteração 100% duplicada → próxima chamada pós-tool com `tools=None`. Testes: dedup executa 1x, loop patológico encerra.
4. **Higiene**: migração `20260712_gemini_3x_pricing.sql` + aplicação idempotente prod/homolog (insert de dados); remover `total_cost_override` do token_tracker; acumulador de usage + resumo de custo no `gemini_actor`/runners de rehearsal.

## Trilha A — Prompts & Tools (executor: subagente, após Trilha B commitada localmente)
5. **TOOLS_SCHEMA**: comprimir descriptions (alvos e pins na spec). Proibido tocar em enums/required/nomes de params.
6. **Base/stage dedup cirúrgico**: apenas redundância verbatim não-pinada (ex.: instruções repetidas entre <constraints> e MODELO DE ESCRITA; frases duplicadas entre regra 33/ORDEM DE EXECUÇÃO/atacado). PROIBIDO: remover CHECKLIST, blocos de preço, circuit breaker, exemplos pinados.

## Validação e deploy
7. `python -m pytest` completo após cada trilha; regressão de aderência → reverter o corte específico e re-rodar até verde.
8. Revisão semântica do diff da Trilha A pela sessão principal (testes não capturam tudo).
9. Commit → `git pull origin master` → `git push origin finops/p2-p3-secagem:master`.
